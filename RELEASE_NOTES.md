# 截图到 Codex v1.1.0

面向 Windows 用户的截图助手：下载、解压、双击安装即可使用，不需要 Python 或 API 密钥。

## 下载与使用

1. 下载下方 **SnipToCodex-v1.1.0-windows-x64.zip**。
2. 右键“全部解压缩”，进入文件夹双击 **Install.cmd**。
3. 打开桌面的 **Snip to Codex**，软件内置三步引导和“查看完整图文教程”按钮。
4. **Ctrl+Alt+S** 框选 → **Enter** 确认 → 确认聊天里出现图片 → 输入问题并发送。

不想安装可运行 **Start.cmd**。如果图片没有自动进入聊天，点击目标输入框按 **Ctrl+V**；网页版也这样使用。

## 本版内容

- 全新使用面板、薄荷绿选区、像素尺寸提示和浮动操作栏。
- 首次使用三步教学，软件内随时可打开中文图文教程。
- 普通用户安装包、桌面和开始菜单快捷方式、免安装入口。
- 使用本机当前用户目录，不依赖作者的电脑路径。
- 可选 Codex 插件，可按用户请求框选并读取截图。
- 截图后自动粘贴可关闭；图片始终由用户自行点击发送。

## 支持范围

Windows 10/11 x64。macOS、Linux 暂不支持；Windows ARM 与混合缩放多显示器尚未验证。桌面版自动粘贴依赖当前输入框结构，失效时可手动粘贴。无需 API 密钥，聊天 AI 需要自己的账户。

程序无网络上传、遥测和后台录屏。二进制目前未签名，可能显示未知发布者；请核对发行来源，校验值在 **SHA256SUMS.txt**。

源码附 MIT 许可证。此项目与 OpenAI 官方无隶属关系。
